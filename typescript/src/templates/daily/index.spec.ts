import Default from '.';
// import { TreeNode } from '../../utils/tree';

describe('Daily', () => {
  it('case 1', () => {
    const input = [];
    const expectedOp = 0;
    expect(Default(input)).toEqual(expectedOp);
  });
});
