import Default from ".";
import tree from "../../utils/tree";

describe("Daily", () => {
  it("case 1", () => {
    const input = [];
    const expectedOp = [];
    expect(Default(input)).toEqual(expectedOp);
  });
});
